namespace Common.Logging
{
    /// <summary>
    /// Represents logging level (verbosity).
    /// </summary>
    public enum LogLevel
    {
        Low = 0,
        Medium = 1,
        High = 2,
        Error = 3
    }
}
